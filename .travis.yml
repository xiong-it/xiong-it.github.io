language: node_js
sudo: required
node_js: 
  - 7.9.0

cache:
  directories:
    - node_modules

branches:
  only:
    - hexo 

before_install:
  - npm install -g hexo-cli

# S: Build Lifecycle
install:
  - npm install
  - npm install hexo-deployer-git --save

before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo generate

after_script:
  - git config user.name "xiongxunxiang"
  - git config user.email "xiongxunxiang_it@163.com"
  - sed 's/baidu_token/${Baidu_Url_Submit_Token}/g' ./config.yml
  - sed 's/githubRepo/${CODING_REPO}/g' ./config.yml
  - sed 's/codingRepo/${GH_REPO}/g' ./config.yml
  - hexo deploy
# E: Build LifeCycle

