language: node_js
sudo: required
node_js: 
  - 7.9.0

cache:
  directories:
    - node_modules

branches:
  only:
    - hexo 

before_install:
  - npm install -g hexo-cli

# S: Build Lifecycle
install:
  - npm install
  - npm install hexo-deployer-git --save

before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo generate

after_script:
  - cd public
  - git init
  - git config user.name "xiongxunxiang"
  - git config user.email "xiongxunxiang_it@163.com"
  - git add .
  - git commit -m "travis-ci auto build."
  - git push -fq https://AndroidMichaelX:@{Coding_TOKEN}@git.coding.net/AndroidMichaelX/AndroidMichaelX.git master:master
  - git push -fq https://${GH_TOKEN}@github.com/xiong-it/xiong-it.github.io.git master:master
  - hexo deploy
# E: Build LifeCycle

